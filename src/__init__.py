"""
This __init__.py file is intentionally left empty.

It marks the 'src' directory as a Python package, allowing modules within it
to be imported using package-relative paths. For example, from 'src/main.py',
you can import a model from 'src/models/user.py' using:

from .models.user import User
"""
